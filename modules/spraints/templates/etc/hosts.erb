##
# Host Database
#
# localhost is used to configure the loopback interface
# when the system is booting.  Do not change this entry.
##
127.0.0.1      localhost <%= scope.lookupvar("::hostname") %>

# The following lines are desirable for IPv6 capable hosts
::1            localhost ip6-localhost ip6-loopback
fe80::1%lo     localhost
ff02::1        ip6-allnodes
ff02::2        ip6-allrouters

<% if @location == "home" %>
192.168.100.52  printer.local
192.168.100.2   router.local
192.168.100.83  airport.local
192.168.0.1     router.att
38.65.252.1     router.hbb
<% end %>
